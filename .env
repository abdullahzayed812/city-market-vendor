# Dev mode — nginx local gateway (port 8080)
# Android emulator: 10.0.2.2 maps to your host machine
# Physical device: replace with your machine's LAN IP (e.g. 192.168.0.128)
API_URL=http://10.0.2.2:8080/api/v1
